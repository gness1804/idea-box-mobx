//      

class Idea {
  constructor(options        ) {
    this.id = Date.now();
    this.name = options.name || '';
    this.body = options.body || '';
    this.quality = options.quality || '';
  }

             
               
               
                  
}

export default Idea;
